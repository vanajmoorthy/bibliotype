# Bibliotype triage progress log

## Codebase Patterns
- Anonymous DNA tasks: bind `task_owner_<task_id>` in cache to
  `request.session.session_key` at upload time (US-001). Session must be
  saved first if `session_key is None` (Django doesn't allocate one until
  first save). Reads of `task_owner_*` happen in `get_task_result_view`
  (US-002) and `claim_anonymous_dna_task` (US-003).
- Test environment: `black` / `isort` are NOT installed in the Docker image
  or the local Poetry venv (not in `pyproject.toml [tool.poetry.group.dev]`,
  which only has `djlint`). Match existing style by hand and skip the
  formatter step until they're added as a dev dep.
- Class-to-module-functions refactor (US-031a recipe): replace
  `class Foo: __init__` defaults with module constants; each
  `def method(self, ...)` becomes `def method(...)`; `self.attr` →
  `MODULE_CONSTANT`; `self.helper()` → `helper()`. `@patch` paths drop the
  `.ClassName` segment: `pkg.mod.ClassName.method` → `pkg.mod.method`. If
  the class had a method AND a same-named convenience function that wrapped
  it with extra behavior (e.g. caching), keep both layers — public name
  stays on the wrapper; pipeline gets an `_uncached` (or similar) underscore
  prefix.

---

2026-05-10: PRD generated from multi-agent triage report (see tasks/prd-codebase-triage-fixes.md).
2026-05-10: Ralph harness scaffolded in scripts/ralph/. prd.json has 70 stories across 8 phases.

## 2026-05-14 14:55 — US-001
- Anonymous branch of `upload_view` now ensures `session_key` exists before
  dispatching the DNA task and writes `task_owner_<task_id>` to Redis with
  TTL 3600s. Added e2e test `test_anonymous_upload_binds_task_owner_to_session`.
- Files changed:
  - core/views.py (anonymous branch of upload_view)
  - core/tests/test_views_e2e.py (new positive test)
  - scripts/ralph/prd.json (US-001 passes=true)
- Learnings for future iterations:
  - AGENTS.md "Redis key registry" section was already pre-populated with
    `task_owner_<task_id>` and the snake_case convention by the PRD third
    pass, so no AGENTS.md edit was needed for this story. Future stories
    should still check AGENTS.md before assuming new sections need writing.
  - `safe_cache_set` is already imported at module top of `core/views.py`
    — no new import required.
  - The test passed in the Docker stack with `manage.py test
    core.tests.test_views_e2e` (8 tests, all green).
---

## 2026-05-14 15:00 — US-002
- `get_task_result_view` now reads `task_owner_<task_id>` and compares against
  `request.session.session_key`. New `ENFORCE_TASK_OWNERSHIP` setting (env
  `ENFORCE_TASK_OWNERSHIP`, default `False`) gates the strict 403 response;
  while it's off, a `task_owner check skipped` warning is logged for each
  unowned lookup so operators can drain pre-US-001 legacy task IDs before
  flipping the switch.
- Files changed:
  - bibliotype/settings.py (new `ENFORCE_TASK_OWNERSHIP` flag)
  - core/views.py (`get_task_result_view` strict/legacy ownership check)
  - core/tests/test_views_e2e.py (3 new tests: owner positive, foreign-session
    strict 403, foreign-session legacy warn-and-allow)
  - scripts/ralph/prd.json (US-002 passes=true)
- Learnings for future iterations:
  - `django.conf.settings` is already imported at the top of `core/views.py`,
    so reading the flag is `settings.ENFORCE_TASK_OWNERSHIP` — no new import.
  - `@override_settings` works on this setting because we centralized it in
    `settings.py` rather than reading `os.environ` at call time inside the
    view. Future story flags should follow the same pattern so tests stay
    deterministic.
  - The legacy-mode test reads `self.csv_file` once for upload and once for a
    fresh `SimpleUploadedFile` to avoid an empty-payload re-read; needed
    because `SimpleUploadedFile` doesn't auto-seek between requests.
  - **Production cutover prerequisite for US-002 (the strict flip)**: deploy
    US-001 first, wait ≥1 hour, confirm no `task_owner check skipped` warnings
    in metrics, then set `ENFORCE_TASK_OWNERSHIP=True`.
- All 11 tests in `core.tests.test_views_e2e` pass.
---

## 2026-05-14 15:10 — US-003
- `signup_view` now (a) verifies POSTed `task_id_to_claim` equals
  `session["anonymous_task_id"]` BEFORE creating the user/calling `login()`,
  and (b) captures `pre_login_session_key = request.session.session_key`
  before login() rotates it. The captured key is passed as the new third
  argument to `claim_anonymous_dna_task.delay(user_id, task_id, session_key)`.
- `claim_anonymous_dna_task` gained an optional `session_key` argument and
  refuses to do any DNA write / UserBook creation when
  `task_owner_<task_id>` is set in cache and the supplied key doesn't match.
  Logs `claim rejected: session_key mismatch` via `logger.warning(extra={...})`.
- Files changed:
  - core/views.py (signup_view: pre-flight task_id check + pre-login key capture)
  - core/tasks.py (claim_anonymous_dna_task: session_key arg + cache verification)
  - core/tests/test_views_e2e.py (2 new: cross-session reject, post-rotation positive)
  - core/tests/test_tasks_integration.py (1 new: direct mismatch invocation)
  - scripts/ralph/prd.json (US-003 passes=true)
- Learnings for future iterations:
  - `core/templates/core/signup.html` does NOT render `form.non_field_errors`,
    only per-field errors. Validation feedback on signup must go through
    `messages.error(request, ...)` (rendered by `base.html`'s messages loop) —
    exactly the existing pattern used for CAPTCHA failures.
  - `assertContains` against a `messages.error` payload should use the raw
    apostrophe ("couldn't"), NOT the HTML-encoded entity. Messages are
    emitted via `{{ message|safe }}` which bypasses auto-escaping.
  - Django `login()` rotates `request.session.session_key`. To verify task
    ownership across login, the pre-login key MUST be captured into a local
    variable BEFORE `login(request, user)` and passed downstream — the
    post-login key will not match the cache.
  - Test pattern for verifying rotation: `pre = client.session.session_key;
    post-login post = client.session.session_key; assertNotEqual(pre, post)`.
    Django's test Client makes both keys observable.
- All 17 tests in `core.tests.test_views_e2e` + `core.tests.test_tasks_integration` pass.
---

## 2026-05-14 15:25 — US-004
- `DEBUG` now defaults to `"False"` in `bibliotype/settings.py`; a startup
  `ImproperlyConfigured("DEBUG must be False in production")` fires when
  `DJANGO_ENV=production` is paired with `DEBUG=True`. `ImproperlyConfigured`
  is imported from `django.core.exceptions` at the top of `settings.py`.
- Local dev container previously relied on the implicit `DEBUG=True` default,
  so `docker-compose.local.yml`'s `web` service now explicitly sets
  `DEBUG=True` and `ALLOWED_HOSTS=localhost,127.0.0.1` (matching the worker
  service block). Without this, `runserver` failed with "You must set
  ALLOWED_HOSTS if DEBUG is False".
- Files changed:
  - bibliotype/settings.py (default flip + production assert + import)
  - docker-compose.local.yml (web service: DEBUG/ALLOWED_HOSTS made explicit)
  - scripts/ralph/prd.json (US-004 passes=true)
- Learnings for future iterations:
  - `manage.py check --deploy` against the local dev container reports only
    pre-existing warnings (W004/W008/W009/W012/W016/W018). The W018 (DEBUG=True)
    warning is expected locally — it's the contract this story enforces in
    production via the explicit assert, not via the deploy-check.
  - Production deploy via `docker-compose.prod.yml` already passes `DEBUG`
    through from the env file; the prod runbook must now set `DEBUG=False`
    explicitly (or omit it — the default is False).
  - AGENTS.md's "Settings invariants" section was pre-populated by the PRD
    third-pass and already documents both the default and the production
    assert. No AGENTS.md edit was needed.
  - **Production cutover note**: ensure the prod `.env` (or whatever supplies
    `DEBUG` to docker-compose.prod.yml) sets `DEBUG=False` and exports
    `DJANGO_ENV=production`. With the new assert, a misconfigured `DEBUG=True`
    + `DJANGO_ENV=production` will refuse to start — by design, fail-loud.
- Full test suite (354 tests) green in Docker stack.
---

## 2026-05-14 16:35 — US-005
- Carved `ADMIN_COMMANDS` + `ALLOWED_COMMANDS` out of `core/admin.py` into a new
  `core/management_command_registry.py` module (single source of truth).
  `core/admin.py` now imports from it (the `ALLOWED_COMMANDS` symbol is the
  same `frozenset` object), and `core/tasks.py` imports it at module top and
  raises `ValueError(f"command not allowed: {command_name}")` as the first
  statement of `run_management_command_task`. This makes the worker reject
  unwhitelisted names even when a caller publishes directly to the broker,
  bypassing the existing HTTP-layer check in `command_run_api`.
- Files changed:
  - core/management_command_registry.py (new — module-level list + frozenset)
  - core/admin.py (deleted inline `ADMIN_COMMANDS`/`ALLOWED_COMMANDS`,
    imports them from the new module)
  - core/tasks.py (`from .management_command_registry import ALLOWED_COMMANDS`
    + first-line whitelist guard in `run_management_command_task`)
  - core/tests/test_tasks_unit.py (new `RunManagementCommandTaskWhitelistTests`
    class: 1 negative test that `call_command` is not invoked on a blocked
    name, 1 empty-string negative test, 1 positive test that a whitelisted
    name reaches `call_command`).
  - core/tests/test_integration.py (two pre-existing tests
    `test_run_management_command_task_captures_output` and
    `..._captures_error` passed arbitrary names `"test_command"` /
    `"failing_command"` and now hit the new whitelist guard; switched both
    to the whitelisted no-op `"analyze_genres"`. Behaviour they assert —
    stdout capture, CommandError wrapping — is unchanged.)
  - scripts/ralph/prd.json (US-005 passes=true)
- Learnings for future iterations:
  - **Don't import from `core.admin` in `core/tasks.py`.** `core/admin.py`
    already does a deferred `from .tasks import run_management_command_task`
    inside `command_run_api`. Lifting `ALLOWED_COMMANDS` to module-top of
    `tasks.py` and importing it from `admin` would invert the dependency at
    Django-startup time when the admin module loads. The new
    `core/management_command_registry.py` module sits below both and is
    imported by both — no cycle.
  - **`@shared_task` tasks are callable as plain Python via `.run(...)`.**
    For pure-logic unit tests (no Celery routing, no broker) prefer
    `task.run(arg)` over `task.apply()` / `task.delay()` — it bypasses the
    EagerResult wrapping so `assertRaises(ValueError)` works without
    `throw=True` / `propagate=True` plumbing. Use `.delay()` only when the
    test specifically needs the Celery dispatcher (e.g. mocking it to
    assert "was this enqueued?").
  - **The `call_command` import inside `run_management_command_task` is
    intentional.** Patching `core.tasks.call_command` from a test would NOT
    work because the name isn't bound at module scope; patch
    `django.core.management.call_command` instead, OR pin the import to
    a known module path. Existing tests use the latter.
  - The HTTP layer (`command_run_api`) still validates `command_name not in
    ALLOWED_COMMANDS` and returns a 400. The task-level guard is the
    defence-in-depth backstop, not a replacement.
- Tests in scope passed: `core.tests.test_tasks_unit core.tests.test_tasks_integration
  core.tests.test_integration` → 67 / 67 green. Full Django suite also exit 0.
---

## 2026-05-19 12:45 — US-006
- Deleted `check_models.py` (throwaway Gemini-model-list script in repo root).
  Pre-deletion grep confirmed no imports outside the PRD / prd.json docs.
- Files changed:
  - check_models.py (removed via `git rm`)
  - scripts/ralph/prd.json (US-006 passes=true)
- Learnings for future iterations:
  - Docker daemon was not running this iteration; fell back to
    `poetry run python manage.py test` (the Bibliotype-specific guardrail
    in these instructions covers this fallback). 362 tests ran; 361 pass,
    1 pre-existing SQLite-locking failure in
    `core.tests.test_math_accuracy.IsbnNormalizationTests.test_round_trip_dedup_goodreads_then_storygraph`.
    The failure is environmental (SQLite under concurrent test access vs the
    canonical Postgres stack) and unrelated to deleting a standalone script
    with no imports — noting here so future iterations don't chase it.
  - For "delete a stray repo-root script" stories (US-007, US-010, US-011,
    US-012, US-013), the cheapest sanity check is `Grep` for the filename
    across the repo with `--exclude-dir=.git`. If matches are only inside
    `tasks/prd-codebase-triage-fixes.md` and `scripts/ralph/prd.json`, the
    file is safe to delete — those are work-list documents, not source.
---

## 2026-05-19 13:00 — US-007
- Verified `git ls-files '*.prof'` returns 0 results and `.gitignore:25` already
  contains `*.prof`. No changes required — pure verification per the story's
  "everything is already clean" branch.
- Files changed:
  - scripts/ralph/prd.json (US-007 passes=true)
- Learnings for future iterations:
  - PR #103 (already merged) handled the actual untracking. Future "verify
    untracked" stories can be one-shot: `git ls-files <glob>` empty + glob
    present in `.gitignore` → mark pass, no code commit.
---

## 2026-05-19 13:15 — US-008a
- Moved 19 synthetic + 5 test_reader CSV fixtures and the three subdirectories
  (`test/`, `test_storygraph/`, `test-csvs/`) from `csv/` into
  `core/tests/fixtures/csv/`. `core/tests/__init__.py` now exposes
  `TEST_FIXTURES_DIR = Path(__file__).parent / "fixtures" / "csv"` for any
  future test that needs a path on disk. (Current tests use
  `SimpleUploadedFile` inline, so no test imports needed updating.)
- Updated `core/management/commands/generate_test_data.py` (the one NOT
  slated for deletion) to write generated CSVs at
  `core/tests/fixtures/csv/...` instead of `csv/...`. The two commands that
  also referenced `csv/...` — `seed_test_books.py` (US-013) and
  `create_users_from_csvs.py` (US-012) — are slated for deletion and were
  intentionally left untouched. The two root-level generator scripts
  `csv/generate_test_data.py` and `csv/generate_synthetic_test_data.py`
  are out of scope (US-009 moves them into management commands).
- Files changed:
  - core/tests/__init__.py (populated `TEST_FIXTURES_DIR` constant)
  - core/tests/fixtures/csv/ (new directory; 24 CSVs + 3 subdirs moved here via `git mv`)
  - csv/ (24 CSVs + 3 subdirs removed by the same `git mv`)
  - core/management/commands/generate_test_data.py (two `csv/` path strings updated)
  - scripts/ralph/prd.json (US-008a passes=true)
- Learnings for future iterations:
  - **Fixtures live at `core/tests/fixtures/csv/`** and the path constant is
    `from core.tests import TEST_FIXTURES_DIR`. Use it whenever a future
    test needs to read a real CSV from disk rather than constructing
    `SimpleUploadedFile` inline.
  - **No test files referenced `csv/` paths at runtime** — confirmed via
    `grep -rn "csv/" core/tests/`. Existing tests all build payloads inline
    with `SimpleUploadedFile`, so the move was test-neutral. This matched
    the PRD's "test files don't actually reference csv/... paths" note.
  - **`git mv` preserves history** across the move — verified `git status`
    after the moves showed renames, not delete+add pairs.
  - Pre-existing SQLite-locking failure in
    `core.tests.test_math_accuracy.IsbnNormalizationTests.test_round_trip_dedup_goodreads_then_storygraph`
    is the only failure (361/362 green) and is unrelated to this story —
    same failure noted in the US-006 entry above. Docker daemon not
    running this iteration; fell back to `poetry run python manage.py test`.
---

## 2026-05-19 13:30 — US-008b
- Personal Goodreads CSVs removed from repo on 2026-05-19; check git history
  if regression analysis ever needs them.
- `git rm` the three tracked personal exports
  (`csv/goodreads_library_export anya.csv`,
  `csv/goodreads_library_export niamh.csv`,
  `csv/goodreads_library_export(1).csv`). `csv/.DS_Store` was untracked, so a
  plain `rm` was sufficient — `git rm` would have errored. `.gitignore` now
  ignores `csv/` (canonical fixtures live in `core/tests/fixtures/csv/` per
  US-008a) and adds `*.DS_Store` alongside the existing `.DS_Store` line.
  `.claude/worktrees/` is already present at `.gitignore:75`, so left
  unchanged.
- `csv/` still contains the two generator scripts
  (`generate_test_data.py`, `generate_synthetic_test_data.py`) tracked from
  before this story; US-009 will `git mv` them into
  `core/management/commands/`. Per AC, did NOT `rmdir csv/` — the directory
  is not empty until US-009 completes.
- Files changed:
  - .gitignore (added `*.DS_Store` after `.DS_Store`; added `csv/` block
    after the "Scraped data and caches" section)
  - csv/goodreads_library_export anya.csv (deleted)
  - csv/goodreads_library_export niamh.csv (deleted)
  - csv/goodreads_library_export(1).csv (deleted)
  - scripts/ralph/prd.json (US-008b passes=true)
- Learnings for future iterations:
  - **Stale references survive in soon-to-be-deleted command files.** `git grep`
    for the deleted CSV names still flags
    `core/management/commands/seed_test_books.py:21` (a default path) and
    `core/management/commands/create_users_from_csvs.py:36` (a docstring
    example). Both files are slated for deletion in US-013 and US-012
    respectively, so leaving the dangling references for one iteration is
    fine — the test suite never exercises these one-shot scripts. Don't
    chase these refs in unrelated stories.
  - **Adding a directory to `.gitignore` does NOT untrack files already
    committed.** `csv/generate_*.py` remain tracked even after `csv/` is
    ignored. `.gitignore` only affects untracked files. To untrack, `git rm
    --cached` or `git mv` is required. US-009 will use `git mv`.
  - **`git rm` errors on untracked paths.** `csv/.DS_Store` was never in
    `git ls-files`, so `git rm csv/.DS_Store` would have failed. Use plain
    `rm` for untracked OS-cruft; AC wording aside, `git rm` is only for
    tracked paths.
  - Docker daemon not running this iteration; fell back to
    `poetry run python manage.py test core.tests.test_views_e2e
    core.tests.test_tasks_unit`. 31 tests green. Did not run the full suite
    (pure file-deletion story with no code paths touched), but the upload +
    task-status surfaces stay green, confirming the personal CSVs weren't
    silently a test fixture.
---

## 2026-05-19 13:45 — US-009
- Ported the two root-level CSV-generator scripts into Django BaseCommands:
  `csv/generate_test_data.py` → `core/management/commands/generate_csvs.py`
  (renamed to avoid the existing `generate_test_data` command),
  `csv/generate_synthetic_test_data.py` →
  `core/management/commands/generate_synthetic_csvs.py`. Original scripts only
  imported BaseCommand cosmetically and ran their logic from `if __name__ ==
  '__main__'`; both are now proper `class Command(BaseCommand)` with
  `add_arguments` (num_users / books_per_user / output_dir / password for
  generate_csvs; output_dir for generate_synthetic_csvs). `manage.py help`
  lists both. The 4 stale `from sys.path / django.setup()` boilerplate lines
  were dropped (BaseCommands run inside the Django process).
- Default output directory is now `core/tests/fixtures/csv/` instead of the
  old `csv/` (consistent with US-008a's canonical fixture path; `csv/` is
  also gitignored after US-008b so writes there would be untracked anyway).
- Files changed:
  - core/management/commands/generate_csvs.py (new BaseCommand)
  - core/management/commands/generate_synthetic_csvs.py (new BaseCommand)
  - csv/generate_test_data.py (deleted via `git rm`)
  - csv/generate_synthetic_test_data.py (deleted via `git rm`)
  - scripts/ralph/prd.json (US-009 passes=true)
- Learnings for future iterations:
  - **Ported scripts had `sys.path.insert(...)` + `django.setup()` cargo
    cult**. BaseCommands run inside the Django process already, so these
    bootstrap lines must be deleted, not migrated. Keeping them in a
    management command works but is dead code that confuses future readers.
  - **The legacy scripts wrote to `csv/` which is now gitignored** (US-008b).
    For any future story that ports a script using paths like
    `csv/goodreads_library_export ....csv`, redirect to
    `core/tests/fixtures/csv/` to keep outputs visible during local dev.
  - **`generate_test_data` (existing core command, kept) vs `generate_csvs`
    (new from this story) are now sibling commands.** They share intent
    (create test_reader{N} users + CSVs) but `generate_csvs` originated from
    the simpler csv/ script. If a future cleanup story dedups them, the
    existing `generate_test_data` is the richer/preferred surface; merge
    into it rather than the other direction.
  - **Pre-existing SQLite-locking failure** in
    `core.tests.test_math_accuracy.IsbnNormalizationTests.test_round_trip_dedup_goodreads_then_storygraph`
    persists this iteration (361/362 green). Same env-only failure noted
    in US-006 and US-008a entries. Not caused by this story; do not chase.
  - Docker daemon not running this iteration; fell back to
    `poetry run python manage.py test` for the full suite.
---

## 2026-05-19 12:30 — US-010
- Deleted unused `catch_all_404_view` from `core/views.py`. The function
  was a literal duplicate of `handler404` (same body) and was never wired
  in any urls.py — `bibliotype/urls.py` already imports/wires
  `handler404` directly.
- Files changed:
  - core/views.py (removed catch_all_404_view + its docstring)
  - scripts/ralph/prd.json (US-010 passes=true)
- Learnings for future iterations:
  - `git grep <symbol>` is the cheapest way to confirm a "verified zero
    references" claim from the PRD before deleting code.
  - The 404 test that mattered for this story is
    `test_nonexistent_user_shows_404_page` in
    `core/tests/test_profile_and_recommendations.py`, not in
    `test_views_e2e.py`. All 10 tests in that module pass without Docker
    (Redis is unused for the 404 path; graceful degradation through
    `safe_cache_*` masks the missing Redis).
  - `test_views_e2e.py` cannot run without Redis (DNA upload tasks
    require the broker for Celery, so the connection failures surface
    as test errors). Use it only inside the Docker stack.
---

## 2026-05-19 14:00 — US-011
- Deleted `core/templates/core/partials/share/card_full.html`. `git grep
  card_full` confirmed only the three themed variants
  (`card_full_miami|neon_picnic|sherbet`) remain, all rendered from
  `core/templates/core/partials/share/share_modal.html`. The bare
  `card_full.html` was never `{% include %}`d anywhere in app code.
- Files changed:
  - core/templates/core/partials/share/card_full.html (deleted via `git rm`)
  - scripts/ralph/prd.json (US-011 passes=true)
- Learnings for future iterations:
  - Pre-flight a deletion with `git grep "<basename>" -- ':!.claude/worktrees'
    ':!scripts/ralph/prd.json' ':!tasks/prd-codebase-triage-fixes.md'` to
    filter out the worktree dirs and the PRD's own self-references. The
    raw `git grep` from the acceptance criteria includes worktree noise.
  - The deleted file was a 4th-style share card with no theme suffix
    (purple background, no themed accents). The three themed siblings
    are full replacements — their structure mirrors `card_full.html`
    almost line-for-line plus theme classes. Future "delete unused
    template" stories: confirm sibling templates haven't been forked
    from the dead one, otherwise the delete is reasonable cleanup.
  - Docker daemon not running this iteration. Ran
    `poetry run python manage.py test core.tests.test_profile_and_recommendations`
    (10 tests, all green) + `manage.py check` (no issues). Template-only
    deletion has no behavioral test surface, so the cheaper scope was
    sufficient. Cannot exercise the share modal render without the
    Docker stack; the markdown PRD's "visit a DNA dashboard locally" step
    is deferred to the next manual sweep.
---

## 2026-05-19 13:30 — US-012
- Deleted three confirmed-dead one-shot management commands:
  `core/management/commands/create_users_from_csvs.py`,
  `core/management/commands/populate_test_dna.py`, and
  `core/management/commands/re_enrich_all_books.py`. Pre-deletion grep
  confirmed each symbol appears only in work-list docs (`progress.txt`,
  `scripts/ralph/prd.json`, `tasks/prd-codebase-triage-fixes.md`) — no
  source imports, no Celery beat schedules, no `call_command` callers.
- `manage.py help` confirms all three are gone from the `[core]` group;
  the US-013 trio (`reset_genres`, `seed_test_books`,
  `seed_mainstream_publishers`) remains listed for next iteration.
- Files changed:
  - core/management/commands/create_users_from_csvs.py (deleted via `git rm`)
  - core/management/commands/populate_test_dna.py (deleted via `git rm`)
  - core/management/commands/re_enrich_all_books.py (deleted via `git rm`)
  - scripts/ralph/prd.json (US-012 passes=true)
- Learnings for future iterations:
  - **`create_users_from_csvs.py` still had a dangling reference noted in
    the US-008b learnings** (docstring example pointing at a deleted
    personal CSV path). Deleting the file makes that note obsolete — no
    follow-up needed in any other file.
  - For management-command deletion stories, the cheapest sanity check is
    `manage.py help` post-`git rm`. If the command name still appears in
    the output, Django is still loading the file from somewhere (typo in
    the path, stale `.pyc`, or a sibling `commands/` dir under a different
    app). Here, all three vanished cleanly — confirming they were
    standalone with no plugin/proxy registration.
  - Pre-existing SQLite-locking failure in
    `core.tests.test_math_accuracy.IsbnNormalizationTests.test_round_trip_dedup_goodreads_then_storygraph`
    persists this iteration (361/362 green). Same environmental issue
    noted in US-006, US-008a, US-009. Docker daemon not running.
---

## 2026-05-19 13:40 — US-013
- Deleted the final batch of confirmed-dead one-shot management commands:
  `core/management/commands/reset_genres.py`,
  `core/management/commands/seed_test_books.py`, and
  `core/management/commands/seed_mainstream_publishers.py`. Pre-deletion
  grep showed each name only in work-list docs (`progress.txt`,
  `scripts/ralph/prd.json`, `tasks/prd-codebase-triage-fixes.md`) plus
  each command's own `.py` file — no `call_command` callers, no Celery
  beat references, no source imports of the `Command` classes.
- `manage.py help` no longer lists any of the three under `[core]`;
  remaining core commands (e.g. `analyze_genres`, `backfill_covers`) are
  still registered. With US-012, this completes the entire dead-command
  cleanup mandated by the triage report.
- Files changed:
  - core/management/commands/reset_genres.py (deleted via `git rm`)
  - core/management/commands/seed_test_books.py (deleted via `git rm`)
  - core/management/commands/seed_mainstream_publishers.py (deleted via `git rm`)
  - scripts/ralph/prd.json (US-013 passes=true)
- Learnings for future iterations:
  - `seed_mainstream_publishers.py` imported `MAINSTREAM_PUBLISHERS_HIERARCHY`
    from `core.dna_constants` — the constant itself stays alive and is
    still used by the genre/publisher classification path. Deleting the
    consumer doesn't orphan the constant, but future cleanup stories that
    propose removing `MAINSTREAM_PUBLISHERS_HIERARCHY` should still grep
    `core/services/` and `core/dna_constants.py` consumers first.
  - `seed_test_books.py` hard-coded a personal CSV path
    (`csv/goodreads_library_export anya.csv`) that was already removed by
    US-008b. The command would have failed at runtime even before this
    deletion — another confirmation it was dead.
  - For management-command deletion stories, `manage.py help | grep
    <name>` is the cheapest post-delete verification, and `manage.py
    check` is sufficient to catch any orphaned import in the remaining
    command graph. No test suite invocation is strictly necessary for a
    pure-file deletion that grep proved untouched.
  - Docker daemon not running this iteration; fell back to scoped
    `poetry run python manage.py test core.tests.test_profile_and_recommendations`
    (10/10 green) + `manage.py check` (0 issues).
---

## 2026-05-19 12:46 — US-014
- Removed two genuinely-unused imports: `Avg` from
  `core/services/user_similarity_service.py:3` (whole import line deleted,
  it was sole symbol) and `Author` from
  `core/services/anonymization_service.py:3` (kept the other two symbols).
  Explicitly left `safe_cache_delete` re-export in
  `core/services/recommendation_service.py:8` untouched (noqa-marked,
  removed only by US-060).
- Files changed:
  - core/services/user_similarity_service.py
  - core/services/anonymization_service.py
  - scripts/ralph/prd.json (US-014 passes=true)
- Learnings for future iterations:
  - Always grep for the symbol in the affected file BEFORE editing imports
    to confirm it's truly unused. `grep -n "Avg" <file>` returns one line
    (the import itself) = safe to remove.
  - Docker not running this iteration → fell back to `poetry run python
    manage.py test core.tests.test_cache_refactor core.tests.test_views_e2e
    core.tests.test_tasks_integration` (50/50 green). The full
    `manage.py test` shows 361 OK + 1 pre-existing SQLite "database table
    is locked" flake in `test_math_accuracy.IsbnNormalizationTests.
    test_round_trip_dedup_goodreads_then_storygraph` — confirmed flake by
    `git stash && rerun` on baseline (also fails). Environment uses
    SQLite when Docker/Postgres absent, surfacing concurrency-unfriendly
    behavior unrelated to import cleanup.
---

## 2026-05-19 13:50 — US-015
- Added production security headers to `bibliotype/settings.py` inside the
  `if not DEBUG:` block: `SECURE_HSTS_SECONDS = 31536000`,
  `SECURE_HSTS_INCLUDE_SUBDOMAINS = True`, `SECURE_HSTS_PRELOAD = True`,
  `SECURE_CONTENT_TYPE_NOSNIFF = True`, `SECURE_REFERRER_POLICY = "same-origin"`.
- Files changed:
  - bibliotype/settings.py (5 lines added inside production block)
  - scripts/ralph/prd.json (US-015 passes=true)
- Learnings for future iterations:
  - To verify deploy-targeted security settings without flipping `.env`,
    run `DEBUG=False poetry run python manage.py check --deploy`. With
    these headers in place the only remaining warning is `security.W020`
    (`ALLOWED_HOSTS must not be empty`) — that one is environmental
    (empty `.env`/`ALLOWED_HOSTS`), unrelated to header config.
  - `manage.py test core.tests` shows the same pre-existing SQLite
    `database table is locked` flake noted in US-014 (one test,
    `test_round_trip_dedup_goodreads_then_storygraph`). Re-run in
    isolation passes. Confirmed not caused by settings change.
  - `black` / `isort` still NOT installed in venv (per Codebase Patterns).
    The 5 added lines match surrounding 4-space indent style by hand.
---

## 2026-05-19 14:05 — US-016
- Added `django-ratelimit ^4.1.0` via `poetry add` (pyproject.toml + poetry.lock).
- Refactored login flow in `core/views.py`: original body moved into
  `_login_view_throttled(request)` decorated with
  `@ratelimit(key="ip", rate="5/m", method="POST", block=True)`. The public
  `login_view(request)` wraps `_login_view_throttled` in `try/except Ratelimited`
  and renders `core/login.html` with `status=429` and a `NON_FIELD_ERRORS`
  entry of "Too many attempts. Please try again in a minute." — picked up by
  the login template's existing `{% if form.non_field_errors %}` block.
- New test file `core/tests/test_login_ratelimit.py` with the two
  AC-mandated tests: 6th POST returns 429 + template error (negative),
  5th POST with valid credentials still authenticates (positive). Both use
  a unique-LOCATION LocMemCache + `cache.clear()` in setUp/tearDown to
  isolate rate-limit counters between tests and from any other test run.
- Files changed:
  - pyproject.toml (django-ratelimit added)
  - poetry.lock (regenerated)
  - core/views.py (decorator stack + Ratelimited handler + 4 new imports)
  - core/tests/test_login_ratelimit.py (new, 2 tests)
  - scripts/ralph/prd.json (US-016 passes=true)
- Learnings for future iterations:
  - **`@ratelimit(..., block=True)` raises `Ratelimited` BEFORE the view body
    runs**, so a try/except inside the decorated view itself can't catch
    it. Two clean patterns: (a) inner-function split with outer wrapper
    catching the exception (what we did here — keeps the decorator visibly
    on the rate-limited function), or (b) `process_exception` middleware
    to convert globally. We picked (a) because it's view-local and avoids
    a global hook; if US-020 (api ratelimit) needs JSON responses we can
    duplicate the pattern there or refactor to middleware later.
  - **`Ratelimited` inherits from `django.core.exceptions.PermissionDenied`**
    in django-ratelimit 4.x, so without our wrapper Django's default 403
    handler would fire — NOT 429. The 429 is our contract; the wrapper is
    essential.
  - **`form.add_error(None, msg)` on an unbound `AuthenticationForm` fails**
    with `AttributeError: 'AuthenticationForm' object has no attribute
    'cleaned_data'`. Django's `add_error` requires `full_clean()` to have
    populated `_errors`/`cleaned_data`. Calling `full_clean()` first adds
    "required" errors for every empty field. The clean workaround is to
    directly assign `form._errors = ErrorDict()` and
    `form._errors[NON_FIELD_ERRORS] = ErrorList([msg])` — the template
    `{{ form.non_field_errors }}` pulls from this dict and renders.
  - **django-ratelimit's IP key uses `request.META["REMOTE_ADDR"]`**, which
    Django's test `Client` defaults to `127.0.0.1`. No `REMOTE_ADDR`
    monkey-patching needed in tests — same-client repeat POSTs accumulate
    against the same counter.
  - **The cache backing `RATELIMIT_USE_CACHE` (default `"default"`) must
    support atomic increment**. `LocMemCache` does, but it's per-process —
    so tests need `cache.clear()` in `setUp`/`tearDown` to isolate. Across
    test classes, override the `LOCATION` for a fresh store.
  - Full suite: 364 tests, 1 pre-existing SQLite-locking flake in
    `test_round_trip_dedup_goodreads_then_storygraph` (same one noted in
    US-006, US-008a, US-009, US-012, US-014). Unrelated to this story.
---

## 2026-05-19 13:30 — US-017
- `clean_email` no longer rejects duplicate emails (returns the value
  unconditionally). `signup_view` now runs an `email__iexact` lookup AFTER
  Turnstile + form validation but BEFORE the US-003 task ownership check:
  on hit, it dispatches a password-reset email via `PasswordResetForm`
  using the same template config as `CustomPasswordResetView`, then
  redirects to the existing `password_reset_done` page so a duplicate
  signup is observationally indistinguishable from a forgotten-password
  request. No new user is created on the duplicate branch.
- Files changed:
  - core/forms.py (clean_email no-op + comment explaining why)
  - core/views.py (imports PasswordResetForm + default_token_generator;
    duplicate-email short-circuit in signup_view)
  - core/tests/test_views_e2e.py (new SignupDuplicateEmailTests class:
    duplicate-email, case-insensitive-duplicate, fresh-email control)
  - scripts/ralph/prd.json (US-017 passes=true)
- Learnings for future iterations:
  - The duplicate-email gate MUST run BEFORE the US-003 task-id check,
    not after. If reordered, an attacker (or a confused user) who POSTs
    a task_id from a different session would hit the task-ownership
    branch FIRST and learn that the email collision exists via the
    branch-specific error message. Keep US-017 gating ahead of any
    other per-user logic in signup.
  - `PasswordResetForm.save()` requires `request=` to compute the
    `{{ domain }}` and `{{ protocol }}` variables in the reset email
    template — passing `None` raises. Always include it.
  - The "check your inbox" page is `password_reset_done` (Django's
    builtin `PasswordResetDoneView` mounted with our template at
    `core/password_reset_done.html`). Reusing it makes the duplicate-
    signup branch and the legitimate forgot-password branch land on
    identical pixels — that's the enumeration defence.
  - Test environment note: `black`/`isort` are still not in the venv;
    matched existing style by hand. The full pre-existing-flake list
    (`Error 61 connecting to localhost:6379` cascade in test_views_e2e
    when Redis is down + the SQLite `test_round_trip_dedup_*` flake)
    is unrelated to this story — confirmed by running the failing tests
    against `git stash`'d main.
---

## 2026-05-19 14:50 — US-018
- Added pre-flight CSV validation to `upload_view`: `pd.read_csv` with
  `nrows=MAX_UPLOAD_ROWS` (50_000) caps memory before the task runs,
  `MAX_UPLOAD_COLUMNS` (100) rejects pathologically wide CSVs, and a
  schema check requires the head columns to contain either
  `{"Title", "Author"}` (Goodreads) or `{"Title", "Authors"}` (StoryGraph).
  When the row cap is hit, the truncated head is re-serialized to CSV so
  the downstream task does not reload the full file. Parsing errors fall
  through to a user-facing "could not be parsed" message.
- New `UploadValidationTests(TransactionTestCase)` class in
  `core/tests/test_views_e2e.py` with 5 tests: oversize-row truncation
  positive, too-many-columns rejection negative, schema-missing rejection
  negative, valid Goodreads positive, valid StoryGraph positive. Each test
  patches `core.views.generate_reading_dna_task` so no Celery / broker
  involvement is required.
- Files changed:
  - core/views.py (StringIO + pandas imports at top; MAX_UPLOAD_ROWS /
    MAX_UPLOAD_COLUMNS constants; pre-flight block inside upload_view)
  - core/tests/test_views_e2e.py (new UploadValidationTests class)
  - scripts/ralph/prd.json (US-018 passes=true)
- Learnings for future iterations:
  - **Truncation strategy:** `nrows=50000` caps pandas memory by design,
    but the downstream task still receives the raw `csv_content` string —
    so the view re-serializes `df_head.to_csv(index=False)` ONLY when the
    head saturates the cap. For sub-cap uploads this preserves the
    original byte stream (including any non-canonical pandas formatting
    the user's tool emitted).
  - **Schema check is case-sensitive on purpose.** Goodreads emits exactly
    `Title` and `Author`; StoryGraph emits `Title` and `Authors`. A
    case-insensitive variant would match malformed exports that the
    downstream parser would silently fail on, so keep `head_columns =
    set(df_head.columns)` and use `.issubset(...)` literally.
  - **`pd.read_csv` raises three distinct exceptions on bad input.**
    `ParserError` for malformed rows, `EmptyDataError` for empty files,
    and a bare `ValueError` for some encoding edge cases. The except
    tuple catches all three; do NOT collapse to `Exception` (would
    swallow programming errors).
  - All 5 new tests + the 12 pre-existing tests in
    `core.tests.test_views_e2e` pass in `poetry run python manage.py test`
    (Docker not running this iteration). The mocked task pattern means
    the negative tests run instantly with no Redis/Celery dependency.
---

## 2026-05-19 14:55 — US-019
- `update_privacy_view` now renders the privacy-toggle success message via
  `render_to_string("core/partials/messages_with_link.html", ...)` instead
  of an f-string that concatenated raw HTML around `request.build_absolute_uri()`.
  The new partial uses `{% url 'core:public_profile' username=username %}`
  for the actual href (relative, never tainted by Host header) and
  auto-escapes `{{ public_url }}` for the visible link text.
- Files changed:
  - core/views.py (import `render_to_string`; rewrite message construction)
  - core/templates/core/partials/messages_with_link.html (new)
  - scripts/ralph/prd.json (US-019 passes=true)
- Learnings for future iterations:
  - `core/templates/core/base.html` displays flash messages with
    `{{ message|safe }}` (line 235). So whatever you put in
    `messages.success(...)` is rendered as HTML — but the safer pattern is
    to pre-render via `render_to_string` so Django auto-escaping runs on
    the dynamic bits at render time. The `|safe` filter in base.html then
    just unwraps the already-escaped HTML.
  - `{% url %}` works fine inside `render_to_string` without passing a
    request — it doesn't need RequestContext, just the URL namespace.
    Verified with a one-liner shell check that outputs `/u/alice/` for the
    href and HTML-escapes `<script>` in the visible text.
  - Manual `dev-browser` check (in PRD AC) was NOT performed this
    iteration — Docker daemon not running. The visual output of the
    rendered partial was verified via a `render_to_string` shell call:
    href resolves to `/u/<username>/`, visible text is escaped. Anyone
    running this iteration through a UI sanity pass should still load
    `/dashboard/`, toggle privacy on, and confirm the link is clickable.
  - Test caveat: `core.tests.test_math_accuracy` has a pre-existing
    SQLite "database table is locked" flake on
    `test_round_trip_dedup_goodreads_then_storygraph` when running the
    full `manage.py test core` suite. Reproduced before my change touched
    anything in the math module — unrelated. `test_views_e2e` (20 tests)
    and `test_profile_and_recommendations` (10 tests) both pass clean
    when run on their own.
---

## 2026-05-19 15:50 — US-020
- `update_username_api` is now rate-limited per-authenticated-user at 10
  POSTs/minute via `django_ratelimit`. Followed the existing
  `_login_view_throttled` two-function pattern: the inner
  `_update_username_api_throttled` carries the `@ratelimit(key="user", ...)`
  decorator; the public `update_username_api` keeps `@login_required` +
  `@require_POST`, calls the throttled body, and catches `Ratelimited` to
  return `JsonResponse({"error": "Too many attempts, try again later."},
  status=429)`.
- Files changed:
  - core/views.py (split throttled inner fn + Ratelimited catch wrapper)
  - core/tests/test_update_username_ratelimit.py (new; 2 tests: 11th POST 429
    + 10th POST normal response)
  - scripts/ralph/prd.json (US-020 passes=true)
- Learnings for future iterations:
  - `key="user"` for `@ratelimit` requires `request.user.is_authenticated`,
    so the outer wrapper MUST keep `@login_required` ahead of any throttled
    call to avoid anonymous KeyError paths. Confirmed by reusing the
    login-view layering pattern.
  - Tests need `@override_settings(RATELIMIT_ENABLE=True, CACHES=...locmem)`
    — same boilerplate as `test_login_ratelimit.py`. There's no global
    `RATELIMIT_ENABLE=True` in `bibliotype/settings.py` yet (rate-limited
    views work in prod because `django_ratelimit` defaults to enabled
    when `cache` is reachable), but each test still sets it explicitly
    AND clears the cache in setUp/tearDown to keep counters isolated.
  - The positive test asserts `status_code != 429` and accepts either
    `200` (valid form save) or `400` (form validation error) on the 10th
    POST because the test posts increasing display-names that may collide
    or fail length validation — the key assertion is "not 429".
  - `black`/`isort` still not installed (see Codebase Patterns); style
    matched by hand against existing test/view patterns.
---

## 2026-05-19 16:25 — US-021
- Lifted `max_workers` from 1 to 8 in the `ThreadPoolExecutor` driving
  `process_book_row` inside `dna_analyser.py:611`. Updated the inline
  comment: the previous `max_workers=1` was a SQLite-era safety; under
  Postgres each worker thread gets its own thread-local connection and
  unique-constraint races on `Author/Book/Genre/Publisher` resolve
  cleanly via `get_or_create`'s IntegrityError-retry path.
- Files changed:
  - core/services/dna_analyser.py (max_workers + comment)
  - scripts/ralph/prd.json (US-021 passes=true)
- Concurrency-safety check (per AC): ran
  `manage.py test core.tests.test_storygraph_integration core.tests.test_tasks_integration`
  5 consecutive times in the Docker stack. All runs produced
  `Ran 26 tests in N.NNNs / OK` (timings 37.8 / 40.2 / 44.7 / 50.6 / 47.5s).
  No order-dependent failures surfaced — concurrent get_or_create races
  on shared Genre/Author rows produced by parallel rows are safely
  serialised by Postgres at the unique-constraint level.
- Learnings for future iterations:
  - The Docker `web`/`worker` images cache the poetry-install layer
    against `pyproject.toml`+`poetry.lock`. If recent stories added a
    dependency (US-016 added `django-ratelimit`) and your local image
    predates that commit, `docker-compose ... up -d` will keep using
    the stale image. Force a rebuild with
    `docker-compose -f docker-compose.local.yml up -d --build web worker`,
    and if `.venv` still looks stale (ModuleNotFoundError) also
    `docker-compose ... rm -f -s -v web worker` to drop the anonymous
    `/app/.venv` volume so the freshly-built image's venv takes over.
  - The Bash tool returned only the *trailing* lines of a long test
    suite output by default; key markers like `Ran N tests / OK`
    landed above the cutoff. Grep the output with
    `grep -E "^(Ran |OK|FAILED|ERROR)"` to reliably surface verdict
    lines instead of relying on `| tail -N`.
  - `ThreadPoolExecutor.map` preserves submission order for the
    yielded results — the loop at line 615 maps DataFrame index ->
    book PK via `read_df.index[row_idx]`, and that invariant still
    holds at `max_workers=8` because `map` yields in input order
    regardless of completion order. Future executors-with-side-effects
    inside this codebase should prefer `map` over `submit/as_completed`
    when ordering matters.
---

## 2026-06-01 16:20 — US-022
- Trimmed 2 redundant per-row queries in `process_book_row`
  (`core/services/dna_analyser.py:481`). The `Genre.objects.filter(books__id=
  book.pk).exists()` on the existing-book branch is now `book.genres.exists()`
  (equivalent freshness, simpler SQL — option (i) per the AC). The full
  `Book.objects.get(pk=book.pk)` refetch after the `F("global_read_count")+1`
  update is now `book.refresh_from_db(fields=["global_read_count"])` — still
  one query but only reloads a single int column AND preserves the FK cache,
  so downstream `book.author.name` lookups skip a lazy-fetch round trip.
  `book.genres.all()` is iterated once into a list before return.
- Files changed:
  - core/services/dna_analyser.py (3 edits inside `process_book_row`)
  - core/tests/test_tasks_integration.py (new
    `test_process_book_row_query_count_regression` sentinel test +
    `CaptureQueriesContext` import)
  - scripts/ralph/prd.json (US-022 passes=true)
- Learnings for future iterations:
  - **`Book.objects.get(pk=book.pk)` returns a NEW model instance with an
    empty FK cache.** Code that touches `fresh_instance.author` (etc.) post-
    refetch silently issues a lazy `SELECT * FROM core_author WHERE id = X`
    query per lookup. `refresh_from_db(fields=[...])` keeps the same Python
    instance and preserves any FK cache from the prior `update_or_create`.
    For this codebase, downstream niche-book code touches `book.author.name`
    in `dna_analyser.py:858` — so preserving the cache saves one query per
    book in addition to the genre-check savings.
  - **Measured saving: 6 queries for a 3-book upload (2 per book).** Test
    sentinel bounds the post-change count at <60 (baseline 56). Pre-change
    was ~62. Future stories that add per-row queries will trip the sentinel
    immediately at small N — keep that bound updated if the DNA pipeline
    grows new per-upload one-shots.
  - **`book.genres.exists()` and `Genre.objects.filter(books__id=book.pk).
    exists()` compile to nearly identical SQL** (same JOIN through the M2M
    table). The reason this story still saves queries on the existing-book
    branch is the COMBINATION with (b) — the FK-cache preservation from
    `refresh_from_db` is where the bulk of the savings actually lives.
  - **`CaptureQueriesContext` is the right tool for query-count assertions
    in `TransactionTestCase` blocks.** `assertNumQueries` would also work
    but pins the count exactly — sentinel-style `assertLess(..., bound)`
    leaves room for unrelated minor optimizations elsewhere.
  - Full Django suite: 381 tests, exit 0 in the Docker stack.
  - Manual canary deferred: AC asks for an end-to-end UI upload, but the
    134-test integration scope (`test_tasks_integration` + `test_storygraph
    _integration` + `test_subtitle_data` + `test_views_e2e` + `test_math_
    accuracy` + `test_fiction_book_extremes`) exercises the same code path
    with a real Postgres backend and validates `most_niche_book.read_count`
    being post-increment (covered explicitly by the new sentinel test). If
    a future runbook step needs a visual confirmation, upload one of the
    fixtures from `core/tests/fixtures/csv/` via the running dev server.
---

## 2026-06-01 16:25 — US-023
- `_save_dna_to_profile` in `core/services/dna_analyser.py` now dispatches
  recommendations via `generate_recommendations_task.delay(profile.user.id)`
  and drops the try/except wrapper — Celery's retry/error path handles task
  failures, and DNA-task latency no longer includes synchronous recs.
- Files changed:
  - core/services/dna_analyser.py (.delay + remove try/except + log update)
  - scripts/ralph/prd.json (US-023 passes=true)
- Learnings for future iterations:
  - Under `CELERY_TASK_ALWAYS_EAGER=True` (set in tests),
    `task.delay(...)` runs the body inline and propagates exceptions when
    `CELERY_TASK_EAGER_PROPAGATES=True` — so removing the try/except did
    not regress the existing recs tests. The Bibliotype test config sets
    both flags (per CLAUDE.md), making `.delay()` behave identically to a
    direct call in tests.
  - The previous inline call carried a stale comment claiming "the caller
    is already in an async task, so there's no benefit to spawning another."
    That reasoning ignored DNA-task latency budgets — the `.delay()` path
    decouples DNA task duration from recs build duration. Future stories
    should be skeptical of comments that justify keeping work synchronous
    inside an already-async task.
- Tests in scope: `core.tests.test_recommendations` + `core.tests.test_tasks_integration`
  → 25/25 green in Docker stack.
---

## 2026-06-01 17:35 — US-024
- `display_dna_view` now sentinel-guards the safety-net recommendations
  dispatch via `safe_cache_add("recs_dispatching_<user_id>", 1, timeout=300)`.
  5 concurrent dashboard polls collapse to 1 task dispatch. The sentinel is
  cleared in `generate_recommendations_task`'s `finally` block, but ONLY on
  terminal exit — on `self.retry(...)` the sentinel is preserved (via a
  `clear_sentinel_on_exit` flag) so duplicate dispatches stay blocked across
  the retry countdown. New `safe_cache_add()` wrapper in `cache_utils.py`
  fails open (returns True) on Redis errors so cache unavailability never
  blocks task dispatch.
- Files changed:
  - core/cache_utils.py (new `safe_cache_add` wrapper)
  - core/views.py (sentinel guard around dispatch in `display_dna_view`)
  - core/tasks.py (terminal-exit sentinel cleanup in `generate_recommendations_task`)
  - core/tests/test_profile_and_recommendations.py (2 new tests:
    5-concurrent-poll collapse + sentinel cleared on task success)
  - scripts/ralph/prd.json (US-024 passes=true)
- Learnings for future iterations:
  - `cache.add()` is atomic in Django's cache framework — returns True only
    when the key was newly set. The `safe_cache_*` family was missing an
    `add` wrapper; future stories that need atomic set-if-absent semantics
    (e.g. distributed locks, dispatch guards) should use `safe_cache_add()`.
    Fail-open on Redis errors matches the rest of the safe wrappers and
    keeps task dispatch from getting stuck when the cache is unavailable.
  - Celery's `self.retry(...)` raises `Retry`, which propagates through
    `try/except Exception`. A naive `finally` block would clear the sentinel
    on retry too, breaking the guard during the retry countdown. The fix is
    a sticky flag set to `False` in the retry-bound except branch BEFORE
    the `raise self.retry(...)`, then checked in `finally`.
  - The `generate_recommendations_task` lazy-imports `safe_cache_delete`
    inside the function body. Moving it to the top of the function (alongside
    the new `safe_cache_delete(f"recs_dispatching_{user_id}")` call) avoids
    a second import in `finally` and keeps both delete sites consistent.
  - Test pattern for `mock.patch` on Celery tasks that the SUT imports lazily:
    patch the canonical reference (`core.tasks.generate_recommendations_task.delay`),
    not the local import inside the view — the view's lazy import binds to
    the patched object at call time.
  - The `cache.clear()` call inside the test is required because
    `LocMemCache` persists state across test methods within the same
    TestCase (it's process-scoped, not request-scoped); without it the
    sentinel from a prior test could leak in.
- Tests in scope: `core.tests.test_profile_and_recommendations` (12/12) +
  `core.tests.test_views_e2e` + `core.tests.test_tasks_integration` (27/27)
  → all green in Docker stack.
---

## 2026-06-01 16:45 — US-025
- Replaced both `AnonymizedReadingProfile.objects.order_by("?")[:100]` call
  sites in `_collect_candidates_for_user` (L486) and
  `_collect_candidates_for_anonymous` (L542) with the AC-specified pattern:
  `ids = list(...values_list("id", flat=True)); sampled = random.sample(ids,
  min(len(ids), 100)); profiles = list(...filter(id__in=sampled))`. Added
  `import random` to the module's stdlib import block. Both sites cache the
  result for 1hr via `anon_profiles_sample`, so the new sample shape is
  identical (list of model instances) — no consumer code change needed.
- Files changed:
  - core/services/recommendation_service.py (import + 2 call sites)
  - scripts/ralph/prd.json (US-025 passes=true)
- Learnings for future iterations:
  - **`order_by("?")` on Postgres compiles to `ORDER BY random()`** which is
    a full-table scan + sort. For tables of any meaningful size this is
    O(N log N) on every cache miss. Sampling ids first is O(N) for the id
    fetch + O(K) for `random.sample`, then an `IN`-clause SELECT which uses
    the primary-key index. Win is biggest as N grows.
  - **`random.sample` raises ValueError when sample size > population**.
    The `min(len(ids), 100)` guard is essential — without it, a fresh DB
    with <100 anonymized profiles would 500 on every recs cache miss.
  - **`values_list("id", flat=True)` on a model with PK `id` is a single
    column index-only scan in Postgres** (cheaper than `values_list("pk")`
    which sometimes triggers a SELECT *). Stick with `"id"` literal.
  - The cached value (`anon_profiles_sample`) is a list of model instances
    pickled into Redis. Switching the sampling strategy keeps the shape
    identical; the only observable difference is that on cache miss the
    100 instances come back via `filter(id__in=...)` in arbitrary PK order
    rather than `random()` order, but downstream code iterates the list
    without ordering assumptions.
- Tests in scope: `core.tests.test_recommendations` (18/18) +
  `core.tests.test_profile_and_recommendations` (12/12) → 30/30 green in
  Docker stack.
---

## 2026-06-01 17:50 — US-026
- `GenreAdmin` and `PublisherAdmin` now override `get_queryset` to annotate
  `book_count_annot=Count("books")`, and the `book_count` display methods
  return the annotation directly. Eliminates N+1 `obj.books.count()` on the
  admin changelist for each row. `@admin.display(ordering=...)` sets
  `admin_order_field` so columns remain sortable.
- Files changed:
  - core/admin.py (Count import + get_queryset on both admins + display
    method bodies)
  - scripts/ralph/prd.json (US-026 passes=true)
- Learnings for future iterations:
  - `Book.genres` (M2M) and `Book.publisher` (FK) both use
    `related_name="books"`, so a single `Count("books")` works for both
    `GenreAdmin` and `PublisherAdmin`.
  - Prefer `@admin.display(description=..., ordering=...)` over assigning
    `.admin_order_field` to the function object — same effect, kept on the
    decorator line. Verified at runtime via `manage.py shell`.
  - Pre-existing failure in
    `core.tests.test_cache_refactor.SaveDnaInvalidationTests.test_save_dna_triggers_recommendation_generation`
    confirmed present on `triage/codebase-fixes` HEAD before this change
    (stashed working tree, reran, still failed). Fallout from US-023 (.delay
    dispatch) that the test wasn't updated to expect. NOT caused by US-026;
    not in scope here. A future story should patch the assertion to
    `mock_rec_task.delay.assert_called_once_with(...)`.
- Tests in scope: full `core.tests` ran (383 tests, 382 pass). Only the
  pre-existing unrelated failure above.
---

## 2026-06-01 17:55 — US-027
- Added `ENABLE_PARALLEL_ENRICHMENT` env flag (default False) to
  `bibliotype/settings.py`. New module-level `_throttle()` helper in
  `core/book_enrichment_service.py` does `time.sleep(1.2)` only when the
  flag is False; the 5 existing `if slow_down: time.sleep(1.2)` sites now
  call `if slow_down: _throttle()`. `enrich_book_task` gained an
  unconditional `rate_limit="30/m"` on its `@shared_task` decorator —
  ships as the safety net regardless of the flag. Parallel enrichment
  lands disabled. Operator must set `ENABLE_PARALLEL_ENRICHMENT=True`
  and validate Open Library + Google Books rate-limit headroom before
  enabling.
- Files changed:
  - bibliotype/settings.py (new `ENABLE_PARALLEL_ENRICHMENT` flag)
  - core/book_enrichment_service.py (`_throttle` helper + 5 call sites)
  - core/tasks.py (`rate_limit="30/m"` on `enrich_book_task`)
  - scripts/ralph/prd.json (US-027 passes=true)
- Learnings for future iterations:
  - The `slow_down=False` default param on each helper already gates the
    sleep at call time; the new `_throttle()` adds a second, settings-level
    gate. The two stack: `slow_down` is the caller's choice (only True from
    the main task path), the settings flag is the operator's choice. Both
    must be True for the sleep to fire — fail-safe by construction.
  - `rate_limit` is a per-worker limit in Celery (30/m means each worker
    processes ≤30 enrichment tasks per minute). With a single worker this
    is conservative; with N workers the effective ceiling is 30N/m. PRD
    chose 30/m as a single-worker default because Open Library's published
    courtesy rate is ~100/m and we want headroom for retries.
  - Existing enrichment tests use `mock.patch` on `requests.Session.get`
    and assert on call counts, not on wall-clock timing. The settings flag
    is invisible to them — they pass under both states. No tests are
    coupled to the sleep behavior.
  - `from django.conf import settings` was not previously imported in
    `book_enrichment_service.py`. Added at the top of the third-party block
    alongside the existing `import requests` (PEP-8 grouping puts Django
    imports with third-party).
- Tests in scope: `core.tests.test_enrichment_stats` +
  `core.tests.test_integration` (59/59) → all green in Docker stack.
- `manage.py makemigrations --check --dry-run`: no changes detected.
---

## 2026-06-01 17:00 — US-027b
- Genre-alias regex patterns now compile **once at module load** of
  `core/book_enrichment_service.py` into `_COMPILED_ALIAS_PATTERNS` (a list
  of `(compiled_pattern, canonical_name)` tuples, sorted longest-alias-first
  to keep "science fiction" winning over "science"). Both
  `_clean_and_canonicalize_genres` and `_canonicalize_google_books_categories`
  now iterate the precompiled list instead of re-running `re.escape` +
  `re.compile` per subject/category per alias.
- Files changed:
  - core/book_enrichment_service.py (module-level constant + both helpers)
  - core/tests/test_integration.py (new `GenreAliasPrecompileTests`)
  - scripts/ralph/prd.json (US-027b passes=true)
- Learnings for future iterations:
  - The PRD's regex literal is `r"\b" + re.escape(a) + r"\b"`; this is
    intentionally a string-concatenation expression (not an f-string) because
    `re.escape` may emit backslashes that an f-string would still
    backslash-quote. Match it verbatim.
  - The list-of-tuples shape `[(pattern, canonical_name), ...]` is critical:
    iterating tuples avoids one dict-lookup per alias per book inside the
    hot loop. Both helpers benefit since they ran the same loop body.
  - Genre-canonicalization tests (`test_integration.test_enrich_book_*`,
    `test_storygraph_integration.test_storygraph_tags_apply_canonical_*`,
    `test_math_accuracy.test_*canonical*`, `test_fiction_book_extremes.*`)
    all pass unchanged — semantics are byte-for-byte equivalent.
  - The new module-load test is intentionally tiny: it asserts the list is
    non-empty and that element 0 is a `(compiled_pattern, str)` tuple. This
    is the canary the PRD asked for — if a future refactor zeroes out the
    list or accidentally swaps element shape, this test fails fast without
    needing a real enrichment fixture.
- Tests in scope: `core.tests.test_integration` + `core.tests.test_enrichment_stats`
  (60/60), plus genre-related suites (`test_math_accuracy`,
  `test_fiction_book_extremes`, `test_storygraph_integration`, 73/73) →
  all green in Docker stack.
---

## 2026-06-01 18:05 — US-024b
- `display_dna_view`'s AnonymousUserSession-recreate fallback now uses
  `update_or_create(session_key=..., defaults={...})` instead of
  `create(...)`. Same payload, but a concurrent dashboard request that
  already inserted the row no longer 500s the second caller on the
  unique-constraint collision.
- Files changed:
  - core/views.py (`display_dna_view` recreate branch, ~L640–L655)
  - core/tests/test_views_e2e.py (new negative test
    `test_anonymous_session_recreation_is_idempotent`)
  - scripts/ralph/prd.json (US-024b passes=true)
- Learnings for future iterations:
  - The recreate fallback is wrapped in `try/except Exception as
    recreate_error: logger.error(...)`. The old `create()` IntegrityError
    would be swallowed and logged, NOT raised — so an idempotency test
    can't `assertRaises(IntegrityError)`. Instead, assert
    `assertNoLogs("core.views", level="ERROR")` while calling the view
    repeatedly under a forced-recreate condition. Confirmed by toggling
    back to `create()` locally: that variant emits the
    "Error recreating anonymous session" ERROR line.
  - To force the recreate branch deterministically, patch the manager
    method directly with `patch.object(AnonymousUserSession.objects,
    "get", side_effect=AnonymousUserSession.DoesNotExist)`. The model is
    imported locally inside `display_dna_view`, so a
    `patch("core.views.AnonymousUserSession")` decorator won't intercept
    it — patching the manager attribute on the class works regardless of
    import site.
  - `get_recommendations_for_anonymous` is also imported locally inside
    the view (from `core.services.recommendation_service`), so mock it at
    the source module path: `patch("core.services.recommendation_service.
    get_recommendations_for_anonymous", return_value=[])`. Otherwise the
    test exercises the entire recommendation engine for no reason.
  - Pattern for "needs a session_key" anonymous tests: `session =
    self.client.session; session["dna_data"] = {...}; session.save()`
    then `session.session_key` is populated. No upload required, which
    keeps the test fast (~0.3s).
- All 21 tests in `core.tests.test_views_e2e` pass.
---

## 2026-06-01 17:10 — US-024c
- `update_recommendation_visibility` now invalidates `user_recommendations_<id>`
  and `similar_users_<id>` on every toggle. When toggling FROM visible TO
  invisible it also flushes `public_users_for_recs_sample` and logs
  `user opted out of recs; cleared candidate sample cache`.
- AGENTS.md Redis registry corrected: the canonical key is
  `user_recommendations_<user_id>` (not `recs_<user_id>` as the prior entry
  claimed) and a new `similar_users_<user_id>` entry was added.
- Files changed:
  - core/views.py (added safe_cache_delete import; cache invalidation logic in update_recommendation_visibility)
  - core/tests/test_profile_and_recommendations.py (new RecommendationVisibilityCacheInvalidationTestCase with 2 tests)
  - AGENTS.md (Redis key registry corrections)
  - scripts/ralph/prd.json (US-024c passes=true)
- Learnings for future iterations:
  - `logger` is already module-level in `core/views.py` (line 51), no import
    of `logging` needed.
  - `safe_cache_delete` was NOT in the existing cache_utils import line —
    only `safe_cache_add/get/set` were imported. Adding it kept the imports
    alphabetised.
  - The PRD's AGENTS.md cross-check was load-bearing: the registry entry for
    `recs_<user_id>` was actually wrong (real key is `user_recommendations_<id>`).
    Future stories that touch caching should reconcile AGENTS.md against the
    real code (`grep` the literal key string) before trusting it.
  - The `was_visible` snapshot must be captured BEFORE `profile.save()` so
    the directional check (visible → invisible) still works.
- All 14 tests in `core.tests.test_profile_and_recommendations` pass.
---

## 2026-06-01 18:16 — US-028
- Added `db_index=True` to `Book.google_books_last_checked` to support
  the enrichment scheduler's polling query. Generated migration
  `0027_alter_book_google_books_last_checked.py` (single AlterField op).
- Files changed:
  - core/models.py (db_index=True on Book.google_books_last_checked)
  - core/migrations/0027_alter_book_google_books_last_checked.py (new)
  - scripts/ralph/prd.json (US-028 passes=true)
- Learnings for future iterations:
  - `makemigrations` correctly produces a single AlterField when toggling
    `db_index` on an existing nullable DateTimeField — no data migration
    needed and the index is built lazily by PostgreSQL.
  - Pre-existing test failure unrelated to this story:
    `core.tests.test_cache_refactor.SaveDnaInvalidationTests
    .test_save_dna_triggers_recommendation_generation` — confirmed by
    re-running on stashed-clean working tree against `fa8e902`. The test
    expects `generate_recommendations_task(self.user.id)` (called as a
    function) but the production code calls `.delay(user_id)`. Likely
    drift from US-023 (which switched the dispatch to `.delay()`). This
    is in the test, not the production code path. Worth flagging when a
    later story touches `_save_dna_to_profile` or that test module.
- 77 tests in `core.tests.test_integration` + `core.tests.test_tasks_integration` + `core.tests.test_views_e2e` pass.
---

## 2026-06-01 18:25 — US-029
- Added composite partial index `userprofile_recs_partial_idx` on
  `UserProfile(visible_in_recommendations, is_public) WHERE dna_data IS NOT NULL`.
  Supports the `find_similar_users` candidate query. Added a `Meta` class to
  `UserProfile` (none existed previously) and imported
  `from django.db.models import Q` to express the partial condition.
  `makemigrations` produced `0028_userprofile_userprofile_recs_partial_idx.py`
  (single AddIndex op).
- Files changed:
  - core/models.py (add `Meta.indexes` with partial index + Q import)
  - core/migrations/0028_userprofile_userprofile_recs_partial_idx.py (new)
  - scripts/ralph/prd.json (US-029 passes=true)
- Learnings for future iterations:
  - Partial indexes via `condition=Q(...)` translate cleanly through Django's
    migration autodetector into PostgreSQL `CREATE INDEX ... WHERE ...` —
    no special migration plumbing required.
  - `UserProfile` previously had no `Meta` class; future stories adding ordering
    or constraints to that model can now extend the new `Meta` block.
  - The pre-existing `test_save_dna_triggers_recommendation_generation`
    failure (flagged in the US-028 entry) is still present and still
    unrelated — re-confirmed against the stashed-clean working tree.
- 28 tests in `core.tests.test_views_e2e` + `core.tests.test_tasks_integration` pass.
---

## 2026-06-01 18:27 — US-030
- Deleted the duplicated `comparative_text` block (35 lines) in
  `core/services/dna_analyser.py` and removed the now-dead key from the
  `dna = { ... }` dict literal. `_enrich_dna_for_display` in `core/views.py`
  is the single source of truth and unconditionally writes
  `length_direction`, `length_pct`, `age_direction`, `age_pct`,
  `bpy_direction`, `bpy_pct` on every render call.
- Added coverage test
  `test_enrich_dna_populates_comparative_text_when_absent` in
  `core/tests/test_number_line.py`. It asserts the helper input lacks
  `comparative_text`, then verifies all six keys are populated by
  `_enrich_dna_for_display`. The pre-existing
  `test_enrich_dna_produces_comparative_text` is retained as a smoke test;
  the new test is stricter (asserts absence first).
- Files changed:
  - core/services/dna_analyser.py (deleted 35-line block + dict key)
  - core/tests/test_number_line.py (new coverage test)
  - scripts/ralph/prd.json (US-030 passes=true)
- Learnings for future iterations:
  - PRD line numbers can drift across iterations. US-030 said "L984–1018"
    but the actual block was at L991–1025 after prior commits. Always
    grep for the symbol (`comparative_text` here) rather than trusting
    line numbers from the PRD.
  - `dna_analyser.calculate_full_dna()` produces the persisted/cached DNA;
    `_enrich_dna_for_display()` re-derives display-only fields at render
    time. Anything that can be re-derived cheaply at render time belongs
    in the enricher, not the analyser — keeps stored DNA smaller and lets
    us evolve display logic without re-running expensive DNA generation.
- All 91 tests in `core.tests.test_views_e2e + test_math_accuracy +
  test_number_line` pass.
---
2026-06-03: regenerate_dna run in production

## 2026-06-03 11:55 — US-031a
- Converted `RecommendationEngine` class to module-level functions in
  `core/services/recommendation_service.py`. `__init__` defaults are now
  module constants (`MIN_SIMILARITY = 0.15`, `QUALITY_THRESHOLD = 3.5`);
  `diversity_factor` removed entirely (was set, never read). Public entry
  points: `get_recommendations_for_user` (caches internally, was the method)
  and `get_recommendations_for_anonymous` (caching wrapper around new
  internal `_get_recommendations_for_anonymous_uncached`).
- Files changed:
  - core/services/recommendation_service.py (class → functions)
  - core/tests/test_cache_refactor.py (engine.X → X, patch path rewrite)
  - core/tests/test_currently_reading.py (5 boost tests rewritten)
  - core/tests/test_recommendations.py (3 fallback tests rewritten)
  - core/tests/test_profile_and_recommendations.py (patch target rewrite)
  - docs/ARCHITECTURE.md (description updated)
  - scripts/ralph/prd.json (US-031a passes=true)
- Learnings for future iterations:
  - The old class had a method `get_recommendations_for_anonymous` (uncached
    pipeline) and a convenience function with the same name (cached wrapper).
    To preserve byte-identical behavior I kept BOTH layers — the public
    `get_recommendations_for_anonymous` is the caching wrapper; the inner
    pipeline lives at `_get_recommendations_for_anonymous_uncached`. Tests
    that bypassed the cache via `engine.get_recommendations_for_anonymous`
    (notably `test_anonymous_cache_returns_empty_for_missing_session`) now
    call the uncached internal directly.
  - The `@patch` rewrite is mechanical: drop the `.RecommendationEngine`
    segment in the dotted path. e.g.
    `core.services.recommendation_service.RecommendationEngine.X` →
    `core.services.recommendation_service.X`. Same goes for `patch()` in a
    `with` block — see the rewrite in
    `test_profile_and_recommendations.py:326` for the pattern.
  - `from .services.recommendation_service import get_recommendations_for_user`
    inside `core/tasks.py:generate_recommendations_task` works fine with the
    new module-function form — the patch in `test_cache_refactor.py` targets
    `core.services.recommendation_service.get_recommendations_for_user`,
    which is the attribute the inline import re-resolves at call time.
- All 388 tests pass.
---

## 2026-06-03 12:05 — US-031b
- Dropped unused parameters from `core/services/recommendation_service.py`:
  `include_explanations` from `get_recommendations_for_user` and
  `_get_recommendations_for_anonymous_uncached` (always True at every call
  site — `_add_explanations` now runs unconditionally), and `is_queryset`
  from `_extract_series_info` (the only caller passed `False`; the function
  now accepts a single list of Book objects). `diversity_factor` was
  already removed during US-031a, so no further change needed there.
- Files changed:
  - core/services/recommendation_service.py
  - scripts/ralph/prd.json (US-031b passes=true)
- Learnings for future iterations:
  - When dropping a kwarg with a default, do a repo-wide grep for the param
    name including in tests. For US-031b, no test passed these kwargs, so
    the change was a pure signature simplification with no call-site edits.
  - `_extract_series_info` had two branches keyed on `is_queryset`: the
    queryset branch handled `ub.book.title` (UserBook), the list branch
    handled `book.title` (Book). Only the list branch was reachable, so
    collapsing the dispatch removed the dead UserBook branch too — no
    inline equivalents in `_build_user_context` need touching because that
    function never called `_extract_series_info`; it iterates `UserBook`
    objects directly with its own series-key logic.
- All 388 tests pass.
---

## 2026-06-03 11:18 — US-032
- `generate_recommendations_task` now bakes `rec["book"]` (the nested dict
  templates expect) and `primary_source_user.badge_class` into
  `processed_recs` before saving to `UserProfile.recommendations_data`.
  `display_dna_view` and `public_profile_view` no longer mutate every rec
  inside an 18-line for-loop — they now run a single guard
  `if "book" not in rec: rec["book"] = _expand_book_dict(rec, BADGE_COLOR_MAP)`
  for dual-shape compat with legacy DB rows. `_expand_book_dict` lives in
  `core/views.py` next to `BADGE_COLOR_MAP`.
- Files changed:
  - core/tasks.py (bake `book` dict + badge_class in storage loop)
  - core/views.py (`_expand_book_dict` helper, collapse both view loops)
  - core/tests/test_cache_refactor.py (new: storage bakes both fields)
  - core/tests/test_recommendations.py (new: dual-shape rendering in
    `display_dna_view` and `public_profile_view`)
  - scripts/ralph/prd.json (US-032 passes=true)
- Learnings for future iterations:
  - **Follow-up note (per US-032 PRD AC):** US-032 ships dual-shape compat.
    After ≥30 days OR after a forced `regenerate_recommendations` run, the
    legacy fallback (`if 'book' not in rec`) can be removed in a follow-up
    story. Earliest safe removal date: 2026-07-03.
  - `core/tasks.py` cannot `from .views import ...` at module top — views
    already does `from .tasks import ...`. The fix is a function-local
    import inside `generate_recommendations_task` (matches the existing
    pattern at `views.py:582`). This is the canonical workaround for
    views↔tasks constant sharing until Phase 8 splits both modules.
  - When baking display-tier fields at storage time, keep the legacy flat
    keys (`book_title`, `book_author`, etc.) — they remain the source of
    truth for the `_expand_book_dict` fallback, so dropping them now would
    silently break any legacy row written before this story shipped.
- All 392 tests pass.
---
