# Phase 1 Regression Report — 20260515-140856

## Environment
- Branch: `triage/codebase-fixes`
- Commit: `e80c734fe7d23e408f70654b53ee754a1f0615f9` (security: fix Phase 1 review blockers + harden ownership checks) — with the working-tree fixes from the user's most recent edits (docker-compose.local.yml worker `DJANGO_ENV`, claim-task deploy-ordering comment, `ENFORCE_TASK_OWNERSHIP` removal, prompt patch, duplicate-test merge) applied on top.
- DEBUG: `True` (web), `True` (worker)
- DJANGO_ENV: `development` (web), `development` (worker) — **F1 fixed**
- Test driver: Chrome DevTools MCP, with curl + Django shell for backend verification

## Summary
- Total tests: 12
- Passed: 12
- Failed: 0
- Skipped: 0

## Verdict: **READY_WITH_NOTES**

Both High-severity findings from the previous run (F1 worker boot regression; F2 stale-claim TypeError) are gone. The full user-visible flow now works end-to-end. One minor pre-existing console error (SVG path typo in `username_editor.html`) and one pre-existing external-API rate-limit noise (Gemini 429) remain — neither is Phase 1 work and both were present in the prior run.

---

## Test results

### T1 — Anonymous CSV upload happy path — **PASS**
- Fresh isolated browser context (`anon_t1`) loaded `/`, attached `csv/goodreads_library_export synthetic_lit_fiction1.csv`, submitted.
- Server logs show `POST /upload/ → 302 → GET /task/a57f522e-c5b0-4ace-9df8-04ef8d53f337/ → 200 → GET /api/task-result/<id>/ → 200`; auto-redirect to `/dashboard/`.
- Dashboard rendered reader type "FANTASY FANATIC", three `<canvas>` elements, top genres/authors, niche read, etc.
- Redis check (`redis-cli -n 1 KEYS '*task_owner*'`): `:1:task_owner_a57f522e-c5b0-4ace-9df8-04ef8d53f337` present → US-001 binding confirmed.
- Screenshots: `screenshots/20260515-140856/t1_home_anon.png`, `t1_dashboard_anon.png`.

### T2 — Authenticated signup (no DNA claim) — **PASS**
- Fresh isolated context (`auth_t2`) navigated to `/signup/`.
- Filled `reg50917` / `reg50917@example.test` / `R3gressionT2!`, submitted.
- Redirect to `/` (correct: code path is `redirect("core:home")`), logout link visible → authenticated.
- Screenshot: `t2_home_after_signup.png`.

### T3 — Authenticated CSV upload — **PASS**
- Same `auth_t2` session uploaded `csv/goodreads_library_export test_reader1.csv` from `/`.
- Auto-polled to `/dashboard/`; rendered `REG50917'S BIBLIOTYPE`, reader type "COMFORT REREADER", three canvases.
- Screenshot: `t3_auth_dashboard.png`.

### T4 — Privacy toggle — **PASS**
- Clicked "Make Public" → dashboard shows banner "Your profile is now public! Share it here: http://localhost:8000/u/reg50917/".
- Anonymous `curl http://localhost:8000/u/reg50917/` → 200 with "Comfort Rereader" body text rendered.
- Clicked "Make Private" → anonymous curl now returns 200 with body containing "private"/"PRIVATE" message.
- Screenshot: `t4_privacy_public.png`.

### T5 — Display name change — **PASS**
- The dashboard inline editor (`core/partials/username_editor.html`) uses `POST /api/update-username/` with JSON body `{"username": "..."}`.
- Submitted `rt348900` → `{"status": "success", "new_username": "rt348900"}` (200).
- Page refresh: h1 now reads `RT348900'S BIBLIOTYPE` — persists.
- (Note: the previous report's `POST /dashboard/update-name/ username=rt54481` path uses the form view; that path also exists but is not driven by any UI.)

### T6 — Logout + login (+ negative) — **PASS**
- `/logout/` → `/` with no logout link → confirmed logged out.
- `/login/` with `reg50917@example.test` / `R3gressionT2!` → `/` with logout link → authenticated.
- Logged out, retried with wrong password → 200 with body "Invalid email or password. Please try again." No traceback / debug page.
- Screenshot: `t6_invalid_login.png`.

### T7 — Cross-session task_id rejection — **PASS**
- Fresh isolated context (`attacker_t7`) issued `GET /api/task-result/a57f522e-c5b0-4ace-9df8-04ef8d53f337/`.
- Response: HTTP **403** body `{"status": "FORBIDDEN"}`.
- Server logs (`web` container):
  - `WARNING views ... task_owner mismatch — cross-session access attempted`
  - `WARNING log ... Forbidden: /api/task-result/a57f522e-c5b0-4ace-9df8-04ef8d53f337/`
- Behaviour is now unconditional (no `ENFORCE_TASK_OWNERSHIP` flag — confirmed removed from `settings.py`, `views.py` comment, `AGENTS.md`, and tests).

### T8 — Signup-time DNA claim — **PASS** (both positive and negative)

**Positive case — F2 regression cleared:**
- Fresh isolated context (`t8_owner`) uploaded `csv/goodreads_library_export synthetic_eclectic1.csv`, captured task_id `2b9dfc0e-7e2e-4dae-9cf8-4eb0bbcbff1e`.
- Navigated to `/signup/?task_id=2b9dfc0e-7e2e-4dae-9cf8-4eb0bbcbff1e` → hidden `task_id_to_claim` field rendered.
- Submitted as `t851746` / `t851746@example.test` → redirect to `/dashboard/`.
- Worker log: `Task core.tasks.claim_anonymous_dna_task[afe0a477-cf90-4743-85a4-874df9b3407c] succeeded in 0.18356545793358237s: None`. **No `TypeError`.** This is the canonical fix evidence.
- Dashboard rendered `T851746'S BIBLIOTYPE`, reader type "COMFORT REREADER" — screenshot `t8_pos_dash_after_signup.png`.
- **(a) Dashboard check:** PASS (above).
- **(b) Fresh re-login check (no warmed session):** opened a new isolated context (`t8_relogin`), logged in fresh as `t851746@example.test` / `C1aimT8Test!`, navigated to `/dashboard/` → renders `T851746'S BIBLIOTYPE` + "COMFORT REREADER"; no "uh oh" placeholder. Screenshot `t8_pos_dash_after_relogin.png`.
- **(c) Django shell DB check:** `UserProfile.dna_data is None?: False`, keys include `['csv_source', 'top_genres', 'user_stats', 'reader_type', 'top_authors', 'longest_book', 'reading_vibe', 'shortest_book']`, `reader_type: 'Comfort Rereader'`, `total_books_read: 29`, `pending_dna_task_id: None`. The claim persisted to the database.

**Negative case (cross-session theft):**
- Fresh isolated context (`t8_attacker`) navigated to `/signup/?task_id=2b9dfc0e-7e2e-4dae-9cf8-4eb0bbcbff1e`, submitted as `atk23491` / `atk23491@example.test`.
- Form re-rendered with error: *"We couldn't verify that this Bibliotype belongs to your current session. Please upload your library again from this browser."*
- Server log: `WARNING views ... signup claim rejected: task_id ownership not verified`.
- Django shell: `attacker_user_exists: False, count: 0` — attacker account NOT created.
- Screenshot: `t8_neg_rejected.png`.

### T9 — Password reset flow — **PASS**
- `/password-reset/` POST with `reg50917@example.test` → 302 → `/password-reset/done/` ("Check your email" page). Screenshot: `t9_reset_done.png`.
- Reset link generated via Django shell (`default_token_generator.make_token` + `urlsafe_base64_encode` on user.pk).
- Visited `/password-reset-confirm/<uidb64>/<token>/` → redirected to `/password-reset-confirm/<uidb64>/set-password/` (Django stashes token in session).
- Submitted `N3wPasswordT9!` (matching confirmation) → `/password-reset-complete/` ("PASSWORD UPDATED").
- Logged in with the new password → `/` with logout link → `/dashboard/` renders `RT348900'S BIBLIOTYPE`.

### T10 — Custom 404 — **PASS**
- `GET /this-page-does-not-exist/` in browser → Django's yellow debug 404 page rendered (`Page not found (404)` + URLconf listing). Expected dev behaviour under `DEBUG=True`; the prompt acknowledges this.
- Django shell verification of `handler404` (with `HTTP_HOST='localhost'` to satisfy `ALLOWED_HOSTS`):
  - `status_code: 404`
  - `has_brand_yellow: True`
  - `has_VT323: True`
  - `has_django_debug: False`
  - `content_len: 13287`
- Production rendering path is intact.

### T11 — Static asset smoke — **PASS**
- `curl /static/dist/output.css` → 200, 52,286 bytes.
- `curl /static/src/input.css` → 200, 3,398 bytes.
- Chrome DevTools network panel on `/dashboard/` reload: 23 stylesheet/script/image/font requests, **zero 4xx**. Chart.js (`https://cdn.jsdelivr.net/npm/chart.js`) → 200. Alpine 3.15.8 → 200. PostHog assets all 200/304. OpenLibrary cover 302s are routine CDN redirects, terminal status 200 on `archive.org` mirrors.
- `document.querySelectorAll('canvas').length` = 3 on dashboard.

### T12 — Console-error sweep — **PASS** (with known pre-existing notes)
- Chrome console errors on `/dashboard/`: 1 error total, all surfaces.
  - `Error: <path> attribute d: Expected number, "…2l8-8zM2 12v2h2l-l-2-2z".` — caused by typo in the SVG path inside `core/templates/core/partials/username_editor.html:26` (`d="M14 0l2 2-2 2-2-2 2-2zM12 2l2 2-8 8H4v-2l8-8zM2 12v2h2l-l-2-2z"`, the `l-l-2-2z` segment has a stray `l`). **Pre-existing, not Phase 1.** Out of scope for this regression but worth filing as a small UI bug.
- No Phase 1 introduced errors. No `TypeError: claim_anonymous_dna_task() takes 3 positional arguments but 4 were given`. No `ImproperlyConfigured`.

---

## Findings status vs. previous run

| Prev finding | Status | Evidence |
|---|---|---|
| **F1** — worker missing `DJANGO_ENV` | **RESOLVED** | `docker-compose.local.yml exec worker env` → `DJANGO_ENV=development`; worker has been running 23h+ with no boot loops, processed claim tasks during this run |
| **F2** — stale Celery worker silent-data-loss on claim | **RESOLVED** | New T8 positive case ran end-to-end; worker log shows `claim_anonymous_dna_task[...] succeeded in 0.18s: None`; `UserProfile.dna_data` populated; fresh re-login dashboard renders DNA |
| **F3** — stale test prompt expectations | **RESOLVED** | Prompt now expects redirect to `/`, unconditional 403 on T7, dev-mode debug 404 on T10, fixture path `csv/` at repo root, no `ENFORCE_TASK_OWNERSHIP` |
| **F4** — dead `ENFORCE_TASK_OWNERSHIP` flag | **RESOLVED** | `grep -r ENFORCE_TASK_OWNERSHIP bibliotype/ core/ AGENTS.md` returns nothing; confirmed by inspection of working tree (settings.py, views.py, tests, AGENTS.md all clean) |
| **F5** — Gemini 429 / Google Books 503 noise | **STILL PRESENT** | Pre-existing external rate limiting; surfaces as "error generating vibe / api call failed" on dashboards. Not Phase 1. |

## New findings

None Phase-1 related. The one Chrome console error noted in T12 is a pre-existing SVG path typo in `core/templates/core/partials/username_editor.html:26` — recommend fixing in a follow-up but does not affect Phase 1 readiness.

## Server log highlights

```
# T7 — desired security path
WARNING views 892 ... task_owner mismatch — cross-session access attempted
WARNING log  892 ... Forbidden: /api/task-result/a57f522e-c5b0-4ace-9df8-04ef8d53f337/

# T8 positive — F2 fix evidence
[INFO/MainProcess]   Task core.tasks.claim_anonymous_dna_task[afe0a477-...] received
[INFO/ForkPoolWorker-1] Task core.tasks.claim_anonymous_dna_task[afe0a477-...] succeeded in 0.184s: None

# T8 negative — defense in depth
WARNING views 892 ... signup claim rejected: task_id ownership not verified
```

No `ImproperlyConfigured`, no `TypeError: claim_anonymous_dna_task() takes 3 positional arguments but 4 were given`. The only worker tracebacks are Gemini API quota errors (F5, pre-existing).

## Recommendations

1. **Phase 2: GO.** Both Phase 1 blockers are cleared.
2. Follow-up (small, not Phase 1): fix the SVG path typo in `core/templates/core/partials/username_editor.html:26` — change `2l8-8zM2 12v2h2l-l-2-2z` to a valid SVG path (looks like `l-l` should be `l-2`, but verify against the intended pencil-icon design).
3. (Out of scope) Gemini 2.5 Flash free-tier quota is regularly exhausted in dev — consider per-dev keys or stubbing in non-prod for clean UX while testing.

---

## Artefacts

Screenshots in `/Users/vanajmoorthy/Desktop/github/bibliotype/scripts/regression/reports/screenshots/20260515-140856/`:
- `t1_home_anon.png`, `t1_dashboard_anon.png`
- `t2_home_after_signup.png`
- `t3_auth_dashboard.png`
- `t4_privacy_public.png`
- `t6_invalid_login.png`
- `t8_pos_dash_after_signup.png`, `t8_pos_dash_after_relogin.png`, `t8_neg_rejected.png`
- `t9_reset_done.png`

Driver: Chrome DevTools MCP, with curl + Django shell for verification of redis bindings, DB persistence, and `handler404`.

`READY / READY_WITH_NOTES / NOT_READY`: **READY_WITH_NOTES**
