# Phase 1 Regression Report — 20260515-150510 (third run)

## Environment
- Branch: `triage/codebase-fixes`
- Commit: `06aa420879d5f6307bea7f878a6f378f5a71e96e` (fix: address Phase 1 regression-test findings (F1-F4))
- ENFORCE_TASK_OWNERSHIP: removed from settings.py in commit `06aa420` (F4)
- DEBUG: `True` on both web and worker; `DJANGO_ENV=development` on both (F1 fix verified — see env grep below)
- Test driver: `curl` + Django shell (Chrome DevTools MCP still unavailable for this session)

## Summary
- Total tests: 12
- **Passed: 12**
- Failed: 0
- Skipped: 0

## Verdict: **READY**

The two High-severity findings from run 121648 (F1 worker boot + F2 stale claim task) are fixed end-to-end. F3 (test prompt freshness) and F4 (dead `ENFORCE_TASK_OWNERSHIP` flag) were also addressed in commit `06aa420`. All 12 tests pass. The branch is ready to ship pending the deploy-ordering caveat in F2 (documented now in `core/tasks.py` docstring).

---

## Test results

| ID | Test | Result | Notes |
|----|------|--------|-------|
| T1 | Anonymous CSV upload happy path | ✅ PASS | 302 → /task/<id>/, task SUCCESS first poll, dashboard 178 KB with "Comfort Rereader" + 3 canvases. Redis key `:1:task_owner_<id>` bound to caller's sessionid (`yenfzd5...`). |
| T2 | Authenticated signup (no DNA claim) | ✅ PASS | 302 → `/` (matches updated test prompt expectation). `logout` link visible. |
| T3 | Authenticated CSV upload | ✅ PASS | 302 → `/dashboard/?processing=true`, dashboard renders on second poll. |
| T4 | Privacy toggle | ✅ PASS | public → `/u/<user>/` returns 200 with DNA; private → 200 with "This profile is private". |
| T5 | Display name change | ✅ PASS | name persists on subsequent dashboard fetches. |
| T6 | Logout + login (incl. wrong-pw) | ✅ PASS | Logout strips auth; correct creds → /; wrong → "Invalid email or password", no traceback. |
| T7 | Cross-session task_id rejection (US-002) | ✅ PASS | HTTP 403 `{"status":"FORBIDDEN"}` + log `WARNING task_owner mismatch — cross-session access attempted`. |
| T8 positive | Signup-time DNA claim (US-003) | ✅ PASS | **F2 fix verified** — `UserProfile.dna_data` populated, `pending_dna_task_id=None`. Fresh login from new cookie jar renders full dashboard (167 KB, "Top Reader Traits" present). Snapshot: `screenshots/20260515-150510/t8_fresh_dash.html`. |
| T8 negative | Cross-session claim theft | ✅ PASS | HTTP 200 form re-render, "We couldn't verify that this Bibliotype belongs to your current session…", attacker User row not created. |
| T9 | Password reset flow | ✅ PASS | Request → /password-reset/done/; shell-generated token confirm → /password-reset-complete/; login with new password → /dashboard/. |
| T10 | Custom 404 | ✅ PASS | Verified via Django shell `handler404(req)`: status 404, contains `brand-yellow` + `VT323`, no Django debug strings. (DEBUG=True in dev still preempts handler404 for real GETs — this is Django behaviour, not a regression. T10 in the test prompt was updated to acknowledge this.) |
| T11 | Static asset smoke | ✅ PASS | 6 Django-served `/static/*` URLs all 200; 3 canvases on dashboard. |
| T12 | Server error sweep | ✅ PASS | No `ImproperlyConfigured` (F1 gone). No `TypeError` on `claim_anonymous_dna_task` (F2 gone). No web errors. Remaining worker tracebacks are pre-existing Gemini quota / Google Books 503s (F5 unchanged). |

---

## F1/F2 verification details

### F1 — worker boot
```
$ docker-compose -f docker-compose.local.yml exec -T worker env | grep -E "DJANGO_ENV|DEBUG"
DJANGO_ENV=development     ← new, fixes the ImproperlyConfigured crash on restart
DEBUG=True

$ docker-compose -f docker-compose.local.yml ps worker
bibliotype-worker-1   ... Up About an hour
```

### F2 — claim task end-to-end
T8 positive replayed without pre-warming `dna_data` via polling:
1. Anonymous upload → task SUCCESS in Redis
2. Signup-with-claim posted directly (no `/api/task-result/` calls in between, so the signup_view's claim_anonymous_dna_task is the *only* DNA-bearing path)
3. After ~6 s sleep:
   ```
   user.id: 44
   has dna_data? True
   pending_dna_task_id: None
   dna_data keys: ['csv_source', 'top_genres', 'user_stats', 'reader_type', 'top_authors', 'longest_book']
   ```
4. Fresh login from a *new* cookie jar → dashboard 167 166 bytes, "Top Reader Traits" rendered.
5. No `TypeError` in worker logs.

This proves the claim flow works end-to-end now. Previous "false positive" pathway (DNA being read from `request.session["dna_data"]` populated by polling) is no longer the source of T8's success.

---

## Caveats

- **Chrome DevTools MCP still unavailable** for this session (same root cause as run 121648 — Claude Code's CLI manages MCP lifecycle; the disconnected server can't be revived in-conversation). All verification was HTTP-level + Django shell. UI pixel rendering not visually verified, but every backend code path was exercised and the canvas DOM elements are confirmed present in the HTML.
- **F2 deploy-ordering risk persists by design.** The fix is documentation (a contributor warning in `core/tasks.py`), not a code change to soften the task signature. The commit message rightly notes that accepting `**kwargs` would re-open the hijack window US-003 closed. So the rolling-deploy requirement must be enforced procedurally. Worth one final scan of any deploy automation that updates web + worker independently.
- Pre-existing Gemini 429 / Google Books 503 errors continue in worker logs (F5 from run 121648, unrelated to Phase 1). User-visible as "still loading" placeholders on enrichment-dependent fields.

---

## Recommendations

1. **Ship it.** Phase 1 (commits `011ab73` → `06aa420`) is end-to-end clean.
2. Ensure the deploy pipeline restarts Celery workers as part of any merge that includes `core/tasks.py` signature changes (the docstring warning is a good belt; a CI lint that diffs `@shared_task` signatures against the previous green main would be the matching braces).
3. Optional: audit `.env.example` and any runbook/CLAUDE.md references for the removed `ENFORCE_TASK_OWNERSHIP` flag.

`READY / READY_WITH_NOTES / NOT_READY`: **READY**
